---
sidebar_position: 4
---

# Stage Color

The stage color is the background color of the stage. It is saved inside the project, so every project can have its own.

The default stage color is white (`#ffffff`). Use the reset button next to the color picker to go back to it.

![example of stage color](/img/stage-color-example.png)

(example of stage color set to `#8fc9ff`)