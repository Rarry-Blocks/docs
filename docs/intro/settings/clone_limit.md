---
sidebar_position: 3
---

# Clone Limit

The clone limit helps keeping the project stable, as having too much clones can slow down the project.

The default clone limit is of `200`. It can be set anywhere from `0` to `100000`.
